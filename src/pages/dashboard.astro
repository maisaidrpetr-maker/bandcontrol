---
// src/pages/dashboard.astro
import NavigationDrawer from '../components/NavigationDrawer.astro';
import Header from '../components/Header.astro';
import OverviewModule from '../components/OverviewModule.astro';
import EventsModule from '../components/EventsModule.astro';
---

<html lang="cs">
<head>
  <meta charset="utf-8" />
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>BandControl</title>
  <style>
    :root {
      --bg-dark: #050505;
      --border-color: #27272a;
      --text-main: #f4f4f5;
    }

    * { box-sizing: border-box; margin: 0; padding: 0; }

    body {
      background-color: var(--bg-dark);
      color: var(--text-main);
      font-family: system-ui, sans-serif;
      min-height: 100vh;
      overflow-x: hidden;
    }

    .dashboard-layout {
      width: 100%;
      display: flex;
      flex-direction: column;
    }

    .modules-rack-row {
      display: flex;
      flex-wrap: wrap;
      gap: 24px;
      padding: 24px;
      align-items: flex-start;
    }

    .compact-hardware-box {
      width: 250px;
      flex: 0 0 250px;
    }

    @media (max-width: 600px) {
      .modules-rack-row { justify-content: center; }
    }
  </style>
</head>
<body>

  <!-- Kontrola přihlášení -->
  <script is:inline>
    if (!sessionStorage.getItem('jePrihlasen')) {
      window.location.href = '/login';
    }
  </script>

  <main id="dashboard-content" class="dashboard-layout">
    <NavigationDrawer />
    <Header />

    <div class="modules-rack-row">
      <div class="compact-hardware-box">
        <EventsModule />
      </div>
      <div class="compact-hardware-box">
        <OverviewModule />
      </div>
    </div>
  </main>

  <!-- Globální odhlašovací logika -->
  <script is:inline>
    document.addEventListener('click', (e) => {
      // Použijeme closest pro jistotu, že zachytíme klik i na vnitřní prvky tlačítka
      if (e.target.closest('#logout-btn')) {
        sessionStorage.clear();
        window.location.href = '/login';
      }
    });
  </script>
</body>
</html>